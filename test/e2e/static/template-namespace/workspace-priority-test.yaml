apiVersion: workspace.jupyter.org/v1alpha1
kind: Workspace
metadata:
  name: test-priority-workspace
  namespace: team-a
spec:
  displayName: "Test Namespace Priority"
  ownershipType: Public
  desiredStatus: Running
  templateRef:
    name: priority-test-template
    # namespace omitted
    # Template exists in BOTH team-a and jupyter-k8s-shared
    # Should use team-a version (workspace namespace priority)
